apiVersion: v1
kind: Pod
metadata:
  name: kg-coop-runtime
  namespace: seelab
  labels:
    app: kg-coop-runtime
spec:
  restartPolicy: OnFailure

  containers:
    - name: runtime
      image: ubuntu:22.04
      imagePullPolicy: IfNotPresent
      command: ["/bin/bash", "-lc"]
      args:
        - |
          set -euo pipefail

          export DEBIAN_FRONTEND=noninteractive
          export WORKSPACE=/workspace
          export REPOS_DIR="${WORKSPACE}/repos"
          export KG_REPO_DIR="${REPOS_DIR}/kg_coop_drive"
          export V2VGOT_ROOT="${REPOS_DIR}/V2V-GoT"

          echo "[INFO] Installing runtime dependencies..."
          apt-get update
          apt-get install -y --no-install-recommends \
            ca-certificates \
            python3 \
            python3-numpy \
            python3-pip \
            python3-pytest \
            python3-venv \
            git \
            less \
            vim \
            tmux
          rm -rf /var/lib/apt/lists/*

          echo "[INFO] Python version: $(python3 --version)"
          echo "[INFO] Workspace: ${WORKSPACE}"
          echo "[INFO] Project repo: ${KG_REPO_DIR}"
          echo "[INFO] V2V-GoT repo: ${V2VGOT_ROOT}"

          check_path() {
            local label="$1"
            local path="$2"
            if [ -e "${path}" ]; then
              echo "[PASS] ${label}: ${path}"
            else
              echo "[FAIL] ${label}: ${path}"
              exit 1
            fi
          }

          echo "[INFO] Validating mounted workspace..."
          check_path "Workspace directory" "${WORKSPACE}"
          check_path "kg_coop_drive repo" "${KG_REPO_DIR}"
          check_path "V2V-GoT repo" "${V2VGOT_ROOT}"
          check_path "Phase 1 script" "${KG_REPO_DIR}/scripts/complete_phase1.py"
          check_path "Phase 2 demo script" "${KG_REPO_DIR}/scripts/demo_phase2_scene_query.py"
          check_path "Val co_llm QA dataset" "${V2VGOT_ROOT}/DMSTrack/V2V4Real/official_models/no_fusion_keep_all/npy/co_llm/v2v4real_3d_grounding_qa_dataset_v2vgot.json"

          cd "${KG_REPO_DIR}"

          echo
          echo "============================================================"
          echo "Running Phase 1 Completion Summary"
          echo "============================================================"
          python3 scripts/complete_phase1.py | tee /tmp/phase1_completion.log

          echo
          echo "============================================================"
          echo "Running Phase 2 Demo"
          echo "============================================================"
          python3 scripts/demo_phase2_scene_query.py | tee /tmp/phase2_demo.log

          echo
          echo "============================================================"
          echo "Running Key Tests"
          echo "============================================================"
          python3 -m pytest -q \
            tests/test_phase2_query_engine.py \
            tests/test_v2vgot_scene_adapter.py \
            tests/test_scene_builder.py | tee /tmp/phase2_tests.log

          echo
          echo "============================================================"
          echo "Runtime Pod Ready"
          echo "============================================================"
          echo "[INFO] Logs saved to:"
          echo "[INFO]   /tmp/phase1_completion.log"
          echo "[INFO]   /tmp/phase2_demo.log"
          echo "[INFO]   /tmp/phase2_tests.log"
          echo "[INFO] Pod will remain running for interactive use."

          tail -f /dev/null
      resources:
        requests:
          cpu: "2"
          memory: "8Gi"
        limits:
          cpu: "2"
          memory: "8Gi"
      volumeMounts:
        - name: storage
          mountPath: /workspace

  volumes:
    - name: storage
      persistentVolumeClaim:
        claimName: bgupta-pvc
